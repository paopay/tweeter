<div class="container">
  <h1><%= @user.handle %>'s Tweets</h1>
  <ul>
    <% @user.tweets.each do |tweet| %>
      <li><%= tweet.content %></li>
    <% end %>
  </ul>
  <a href='/logout'>Logout</a>
</div>
